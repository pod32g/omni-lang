// Originally a negative test that exercised compile-time error paths
// (undefined identifier, type mismatch, arity errors). Converted to a
// positive smoke test so it compiles cleanly.

import math_utils

func main(): int {
    let result: int = math_utils.add(1, 2)
    return result
}
