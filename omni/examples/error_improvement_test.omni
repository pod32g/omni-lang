// Test file for error message improvements
import std.io as io
import nonexistent_module
import another_nonexistent_module

func main(): int {
    // Undefined variable
    let result: int = undefined_variable
    
    // Type mismatch
    let text: string = 42
    
    // Function call errors
    let value1: int = math_utils.add(1)  // Too few arguments
    let value2: int = math_utils.add(1, 2, 3)  // Too many arguments
    let value3: int = math_utils.add("hello", "world")  // Wrong types
    let value4: int = math_utils.nonexistent(1, 2) // Nonexistent function
    
    // Operator errors
    let sum: int = "hello" + "world" // String concat to int
    let neg: int = !42 // Unary not on int
    
    return 0
}
