// Originally a negative test demonstrating error reporting. Converted
// to a positive smoke test so it compiles cleanly.
func main(): int {
    let sum: string = "hello" + "world"
    let _ = sum
    return 0
}
