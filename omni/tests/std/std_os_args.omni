import std

func main():int {
    let count = std.os.args_count()
    if count != 2 {
        return -1
    }

    if std.os.positional_arg(0, "") != "hello" {
        return -2
    }
    if std.os.positional_arg(1, "") != "world" {
        return -3
    }
    return 0
}
