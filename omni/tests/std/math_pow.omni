func main():int {
    return pow(2, 3)
}

func pow(base:int, exp:int):int {
    if exp < 0 {
        return 0
    }
    if exp == 0 {
        return 1
    }
    let result:int = 1
    for i:int = 0; i < exp; i++ {
        result = result * base
    }
    return result
}
