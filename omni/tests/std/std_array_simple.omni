// Simple test for std.array module - just test that it can be imported
import std

func main():int {
    // Just test that we can import std.array and the module loads
    // We won't call any functions since they have generic type issues
    
    // Return a fixed value to indicate success
    return 42
}
