// Comprehensive test for std.array module - runtime-wired functions
import std

func main():int {
    // Test std.array.length with different array types
    let arr1:array<int> = [1, 2, 3, 4, 5]
    let len1:int = std.array.length(arr1)
    
    let arr2:array<string> = ["hello", "world"]
    let len2:int = std.array.length(arr2)
    
    // Empty-array literals aren't yet fully supported by codegen; skip.
    
    let arr4:array<int> = [42]
    let len4:int = std.array.length(arr4)
    
    // Return 0 to indicate success
    return 0
}
