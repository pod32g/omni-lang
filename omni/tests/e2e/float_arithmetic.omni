func main():int {
    let a: float = 3.14
    let b: float = 2.86
    let sum: float = a + b
    let diff: float = a - b
    let product: float = a * b
    let quotient: float = a / b
    
    // Test comparisons
    if sum > 5.0 {
        if diff < 1.0 {
            if product > 8.0 {
                if quotient > 1.0 {
                    return 42
                }
            }
        }
    }
    return 0
}
