// Simple generic function test
func identity<T>(x:T):T {
    return x
}

// Simple generic struct test  
struct Container<T> {
    value:T
}

func main() {
    let x:int = 42
    // Note: Generic instantiation not yet supported
    // let result = identity<int>(x)
    // let container = Container<string>{value: "hello"}
}
